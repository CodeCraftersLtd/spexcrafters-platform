package com.spexcrafters.taxonomy.domain;

/** Unit-of-measure family (domain-model §5). Mirrors {@code ck_unit_of_measure_family}. */
public enum UnitFamily {
    LENGTH,
    MASS,
    POWER_DIOPTER,
    ANGLE,
    COUNT
}
