/**
 * Public API of the organizations bounded context.
 *
 * <p>Sprint-1 stub. The organizations module (Organization, Membership, Invitation, OrgRole)
 * is scheduled for Sprint 2; this package exists so the module boundary, Maven coordinates and
 * ArchUnit rules are in place from the start. Cross-module access is only permitted through
 * this {@code ...api} package.
 */
package com.spexcrafters.organizations.api;
