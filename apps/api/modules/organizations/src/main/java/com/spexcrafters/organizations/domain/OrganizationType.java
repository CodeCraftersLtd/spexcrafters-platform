package com.spexcrafters.organizations.domain;

/** The market side an organization operates on. Wire values per the OpenAPI contract. */
public enum OrganizationType {
    BUYER,
    SUPPLIER,
    HYBRID
}
