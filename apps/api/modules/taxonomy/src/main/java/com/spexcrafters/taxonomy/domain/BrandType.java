package com.spexcrafters.taxonomy.domain;

/** Brand classification (domain-model §8). Mirrors {@code ck_brand_type}. */
public enum BrandType {
    LENS,
    FRAME,
    SUNGLASSES,
    CONTACT_LENS,
    MACHINE,
    ACCESSORY,
    GENERAL
}
